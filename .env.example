# Gemini (Baby Name AI) — copy from CareerMateAI `.env` or create a key at
# https://aistudio.google.com/apikey
#
# Never commit the real `.env` file.
GEMINI_API_KEY=
# Optional preferred model (otherwise the app tries a fallback list)
GEMINI_MODEL=
